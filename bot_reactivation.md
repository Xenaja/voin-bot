# Ре-активационная рассылка «Код Воина» → клуб v2 (WIP, июль 2026)

**Статус: не запущено.** Код дописан 13.07.2026, лежал незакоммиченным в рабочем
дереве до 23.09.2026, боевого прогона не было. Бот v1 на момент коммита не активен,
поэтому цепочку не разворачивали. Сид не выполнялся — таблица `reactivation` пустая.

Задача: добрать покупателей гайда «Код Воина» (бот v1) в клуб «Первый шаг» (бот v2,
@VoinKodBot). Только Telegram — в VK цепочки нет.

## Как работает

Цепочка из 5 сообщений, шаг = сколько уже отправлено (0..5).

| Шаг | Что | Кнопка |
|---|---|---|
| 0 | крючок | — |
| 1 | мостик к клубу | — |
| 2 | оффер (690 ₽/мес) | URL на `?start=reactivate` |
| 3–4 | дожимы | URL на `?start=reactivate` |

Задержки в часах от прошлого сообщения — `config.REACTIVATE_DELAYS_H = [0, 24, 24, 72, 96]`,
то есть дни 0 / 1 / 2 / 5 / 9. Тик планировщика — раз в час.

## Файлы

- `core/reactivation.js` — собирает сообщение шага; с шага 2 добавляет `urlButton`.
- `core/messages.js` — тексты `REACTIVATION[0..4]` и подпись кнопки `BTN_REACTIVATE`.
- `core/scheduler.js` — часовой `setInterval`: по каждому шагу берёт созревшие строки,
  отправляет, сдвигает шаг.
- `core/store.js` — таблица `reactivation` и методы `getDueReactivation`,
  `advanceReactivation`, `markReactivationPaid`, `isClubMemberInV2`.
- `adapters/telegram.js` — поддержка `msg.urlButton` (inline-кнопка со ссылкой);
  раньше умел только `callback`.
- `config.js` — `REACTIVATE_BOT_LINK`, `REACTIVATE_DELAYS_H`.
- `seed-reactivation.js` — разовый сид: все `state='COMPLETED'` + `platform='telegram'`,
  за вычетом тех, кто уже в клубе v2.
- `test-reactivation-send.js` — шлёт все 5 сообщений одному chat_id, в БД не пишет.

## Таблица `reactivation`

```
chat_id TEXT UNIQUE, step INTEGER, updated_at TEXT, paid INTEGER
```

`paid=1` — вступил в клуб, дожимы прекращаются.

## Связь с ботом v2

`store.isClubMemberInV2()` читает БД второго бота **только на чтение**, по умолчанию
`../../voin-bot-v2/data/users.db` (переопределяется `V2_DB_PATH`); ищет
`state='COMPLETED_CLUB'`. Telegram-id общий для обоих ботов, поэтому связка идёт по нему.
Если БД недоступна — возвращает `false`, то есть рассылка не блокируется, но и оплативший
может получить лишний дожим. Перед запуском проверить путь: папки v1 и v2 должны лежать
рядом.

## Что сделать перед запуском

1. Node 22 на сервере — используется встроенный `node:sqlite`.
2. Проверить тексты `REACTIVATION` и цену: в шаге 2 зашито 690 ₽/мес.
3. Прогнать `node test-reactivation-send.js <свой chat_id>` — увидеть все 5 сообщений.
4. Убедиться, что путь к БД v2 верный (иначе оплатившие получат дожимы).
5. Только потом `node seed-reactivation.js` — он сразу ставит первое сообщение в очередь
   на ближайший часовой тик.

## Известные шероховатости

- Ошибка отправки (юзер заблокировал бота) **сдвигает шаг**, а не ставит на паузу, —
  сообщение для него теряется. Сделано, чтобы не долбить в цикле.
- Ночная пауза 21:00–09:00 МСК считается через сдвиг UTC+3 в `getDueReactivation`,
  переход на другое смещение не предусмотрен.
- `paid` выставляется только в момент тика: между оплатой и следующим тиком (до часа)
  дожим ещё может уйти.
